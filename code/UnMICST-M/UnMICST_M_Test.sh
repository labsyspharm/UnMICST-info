python -u -m torch.distributed.launch --master_port=9511 --nproc_per_node=1 --use_env UnMICST_M_Train.py --world-size 1 --use-channel <dapi/both> --test-only --testdomain <clean/topblur/bottomblur> --resume <path to a saved model> --root-path <path to dataset folder> --output-dir <path to save segmentation results>
